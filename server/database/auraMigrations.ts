import crypto from'node:crypto';import type{Pool,PoolClient}from'pg';
export interface AuraMigration{id:string;description:string;sql:string;checksum:string}const SQL=`
CREATE TABLE IF NOT EXISTS aura_agent_workflows(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,source_mode TEXT NOT NULL,locale TEXT NOT NULL,prompt TEXT NOT NULL DEFAULT '',template_id TEXT,product_id TEXT,market_context JSONB NOT NULL DEFAULT '{}'::jsonb,creative_context JSONB NOT NULL DEFAULT '{}'::jsonb,current_step TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'active',idempotency_key TEXT,version INTEGER NOT NULL DEFAULT 1,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE UNIQUE INDEX IF NOT EXISTS idx_aura_workflow_idempotency ON aura_agent_workflows(workspace_id,user_id,idempotency_key) WHERE idempotency_key IS NOT NULL;CREATE INDEX IF NOT EXISTS idx_aura_workflow_active ON aura_agent_workflows(workspace_id,user_id,status,updated_at DESC);
CREATE TABLE IF NOT EXISTS aura_campaign_briefs(id TEXT PRIMARY KEY,workflow_id TEXT NOT NULL REFERENCES aura_agent_workflows(id) ON DELETE CASCADE,workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,version INTEGER NOT NULL,revision INTEGER NOT NULL DEFAULT 1,is_latest BOOLEAN NOT NULL DEFAULT TRUE,status TEXT NOT NULL DEFAULT 'draft',brief JSONB NOT NULL,evidence JSONB NOT NULL,warnings JSONB NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(workflow_id,version));CREATE INDEX IF NOT EXISTS idx_aura_campaign_brief_latest ON aura_campaign_briefs(workflow_id,is_latest,version DESC);
CREATE TABLE IF NOT EXISTS aura_campaign_content_packages(id TEXT PRIMARY KEY,workflow_id TEXT NOT NULL REFERENCES aura_agent_workflows(id) ON DELETE CASCADE,brief_id TEXT NOT NULL REFERENCES aura_campaign_briefs(id) ON DELETE CASCADE,workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,version INTEGER NOT NULL,is_latest BOOLEAN NOT NULL DEFAULT TRUE,status TEXT NOT NULL DEFAULT 'generated',locale TEXT NOT NULL,payload JSONB NOT NULL,provider TEXT NOT NULL,model TEXT NOT NULL,prompt_tokens INTEGER,completion_tokens INTEGER,latency_ms INTEGER NOT NULL,credits_charged INTEGER NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(workflow_id,version));CREATE INDEX IF NOT EXISTS idx_aura_content_package_latest ON aura_campaign_content_packages(workflow_id,is_latest,version DESC);CREATE TABLE IF NOT EXISTS aura_campaign_content_requests(id TEXT PRIMARY KEY,workflow_id TEXT NOT NULL REFERENCES aura_agent_workflows(id) ON DELETE CASCADE,brief_id TEXT NOT NULL REFERENCES aura_campaign_briefs(id) ON DELETE CASCADE,workspace_id TEXT NOT NULL,user_id TEXT NOT NULL,idempotency_key TEXT NOT NULL,status TEXT NOT NULL,package_id TEXT,error_message TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(workspace_id,user_id,idempotency_key));
CREATE TABLE IF NOT EXISTS aura_creative_directions(id TEXT PRIMARY KEY,workflow_id TEXT NOT NULL,brief_id TEXT NOT NULL,content_package_id TEXT NOT NULL,workspace_id TEXT NOT NULL,user_id TEXT NOT NULL,version INTEGER NOT NULL,revision INTEGER NOT NULL DEFAULT 1,is_latest BOOLEAN NOT NULL DEFAULT TRUE,status TEXT NOT NULL DEFAULT 'draft',locale TEXT NOT NULL,direction JSONB NOT NULL,provider TEXT NOT NULL,model TEXT NOT NULL,prompt_tokens INTEGER,completion_tokens INTEGER,latency_ms INTEGER NOT NULL,credits_charged INTEGER NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(workflow_id,version));CREATE INDEX IF NOT EXISTS idx_aura_creative_direction_latest ON aura_creative_directions(workflow_id,is_latest,version DESC);CREATE TABLE IF NOT EXISTS aura_creative_direction_requests(id TEXT PRIMARY KEY,workflow_id TEXT NOT NULL,brief_id TEXT NOT NULL,content_package_id TEXT NOT NULL,workspace_id TEXT NOT NULL,user_id TEXT NOT NULL,idempotency_key TEXT NOT NULL,status TEXT NOT NULL,direction_id TEXT,error_message TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(workspace_id,user_id,idempotency_key));
CREATE TABLE IF NOT EXISTS aura_campaign_exports(id TEXT PRIMARY KEY,workflow_id TEXT NOT NULL,brief_id TEXT NOT NULL,content_package_id TEXT NOT NULL,creative_direction_id TEXT NOT NULL,workspace_id TEXT NOT NULL,user_id TEXT NOT NULL,version INTEGER NOT NULL,is_latest BOOLEAN NOT NULL DEFAULT TRUE,idempotency_key TEXT NOT NULL,locale TEXT NOT NULL,manifest JSONB NOT NULL,bundle JSONB NOT NULL,sha256 TEXT NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(workflow_id,version),UNIQUE(workspace_id,user_id,idempotency_key));CREATE INDEX IF NOT EXISTS idx_aura_campaign_export_latest ON aura_campaign_exports(workflow_id,is_latest,version DESC);
CREATE TABLE IF NOT EXISTS aura_production_blueprints(id TEXT PRIMARY KEY,workflow_id TEXT NOT NULL,brief_id TEXT NOT NULL,content_package_id TEXT NOT NULL,creative_direction_id TEXT NOT NULL,workspace_id TEXT NOT NULL,user_id TEXT NOT NULL,version INTEGER NOT NULL,revision INTEGER NOT NULL DEFAULT 1,is_latest BOOLEAN NOT NULL DEFAULT TRUE,status TEXT NOT NULL DEFAULT 'draft',idempotency_key TEXT NOT NULL,locale TEXT NOT NULL,blueprint JSONB NOT NULL,sha256 TEXT NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(workflow_id,version),UNIQUE(workspace_id,user_id,idempotency_key));CREATE INDEX IF NOT EXISTS idx_aura_production_blueprint_latest ON aura_production_blueprints(workflow_id,is_latest,version DESC);
CREATE TABLE IF NOT EXISTS aura_media_asset_batches(id TEXT PRIMARY KEY,workflow_id TEXT NOT NULL,blueprint_id TEXT NOT NULL,workspace_id TEXT NOT NULL,user_id TEXT NOT NULL,version INTEGER NOT NULL,status TEXT NOT NULL DEFAULT 'planned',idempotency_key TEXT NOT NULL,provider TEXT NOT NULL,estimated_credits INTEGER NOT NULL DEFAULT 0,credits_charged INTEGER NOT NULL DEFAULT 0,credits_refunded INTEGER NOT NULL DEFAULT 0,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(workflow_id,version),UNIQUE(workspace_id,user_id,idempotency_key));CREATE TABLE IF NOT EXISTS aura_media_asset_jobs(id TEXT PRIMARY KEY,batch_id TEXT NOT NULL REFERENCES aura_media_asset_batches(id) ON DELETE CASCADE,workspace_id TEXT NOT NULL,user_id TEXT NOT NULL,slot_id TEXT NOT NULL,target_id TEXT NOT NULL,scene_id TEXT NOT NULL,kind TEXT NOT NULL,status TEXT NOT NULL,prompt TEXT NOT NULL,negative_prompt TEXT NOT NULL,aspect_ratio TEXT NOT NULL,width INTEGER NOT NULL,height INTEGER NOT NULL,required BOOLEAN NOT NULL,evidence_refs JSONB NOT NULL,provider TEXT,model TEXT,storage_key TEXT,content_type TEXT,size_bytes INTEGER,sha256 TEXT,error_code TEXT,attempts INTEGER NOT NULL DEFAULT 0,credits_charged INTEGER NOT NULL DEFAULT 0,started_at TIMESTAMPTZ,completed_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(batch_id,slot_id));CREATE INDEX IF NOT EXISTS idx_aura_media_batch_latest ON aura_media_asset_batches(workflow_id,version DESC);CREATE INDEX IF NOT EXISTS idx_aura_media_jobs_batch ON aura_media_asset_jobs(batch_id,status,created_at);
CREATE TABLE IF NOT EXISTS aura_video_render_batches(id TEXT PRIMARY KEY,workflow_id TEXT NOT NULL,blueprint_id TEXT NOT NULL,media_batch_id TEXT NOT NULL,workspace_id TEXT NOT NULL,user_id TEXT NOT NULL,version INTEGER NOT NULL,status TEXT NOT NULL DEFAULT 'planned',idempotency_key TEXT NOT NULL,estimated_credits INTEGER NOT NULL DEFAULT 0,credits_charged INTEGER NOT NULL DEFAULT 0,credits_refunded INTEGER NOT NULL DEFAULT 0,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(workflow_id,version),UNIQUE(workspace_id,user_id,idempotency_key));CREATE TABLE IF NOT EXISTS aura_video_render_jobs(id TEXT PRIMARY KEY,batch_id TEXT NOT NULL REFERENCES aura_video_render_batches(id) ON DELETE CASCADE,workspace_id TEXT NOT NULL,user_id TEXT NOT NULL,target_id TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'planned',progress INTEGER NOT NULL DEFAULT 0,attempts INTEGER NOT NULL DEFAULT 0,plan JSONB NOT NULL,storage_key TEXT,sha256 TEXT,size_bytes INTEGER,width INTEGER,height INTEGER,duration_seconds DOUBLE PRECISION,codec TEXT,container TEXT,error_code TEXT,credits_reserved INTEGER NOT NULL DEFAULT 0,credits_charged INTEGER NOT NULL DEFAULT 0,credits_refunded INTEGER NOT NULL DEFAULT 0,started_at TIMESTAMPTZ,completed_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(batch_id,target_id));CREATE INDEX IF NOT EXISTS idx_aura_video_batch_latest ON aura_video_render_batches(workflow_id,version DESC);CREATE INDEX IF NOT EXISTS idx_aura_video_jobs_batch ON aura_video_render_jobs(batch_id,status,created_at);
CREATE TABLE IF NOT EXISTS aura_video_reviews(id TEXT PRIMARY KEY,workflow_id TEXT NOT NULL,render_batch_id TEXT NOT NULL,blueprint_id TEXT NOT NULL,workspace_id TEXT NOT NULL,user_id TEXT NOT NULL,version INTEGER NOT NULL DEFAULT 1,status TEXT NOT NULL DEFAULT 'draft',review JSONB NOT NULL,idempotency_key TEXT NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(workflow_id,render_batch_id),UNIQUE(workspace_id,user_id,idempotency_key));CREATE INDEX IF NOT EXISTS idx_aura_video_review_latest ON aura_video_reviews(workflow_id,updated_at DESC);
CREATE TABLE IF NOT EXISTS aura_video_scene_revision_jobs(id TEXT PRIMARY KEY,review_id TEXT NOT NULL REFERENCES aura_video_reviews(id) ON DELETE CASCADE,revision_id TEXT NOT NULL,workspace_id TEXT NOT NULL,user_id TEXT NOT NULL,source_render_job_id TEXT NOT NULL,target_id TEXT NOT NULL,scene_id TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'planned',attempts INTEGER NOT NULL DEFAULT 0,plan JSONB NOT NULL,replacement_image_key TEXT,output_video_key TEXT,sha256 TEXT,size_bytes INTEGER,error_code TEXT,ai_credits_reserved INTEGER NOT NULL DEFAULT 0,video_credits_reserved INTEGER NOT NULL DEFAULT 0,ai_credits_charged INTEGER NOT NULL DEFAULT 0,video_credits_charged INTEGER NOT NULL DEFAULT 0,ai_credits_refunded INTEGER NOT NULL DEFAULT 0,video_credits_refunded INTEGER NOT NULL DEFAULT 0,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),completed_at TIMESTAMPTZ,UNIQUE(review_id,revision_id));CREATE INDEX IF NOT EXISTS idx_aura_scene_revision_review ON aura_video_scene_revision_jobs(review_id,status,created_at);
CREATE TABLE IF NOT EXISTS aura_production_deliveries(id TEXT PRIMARY KEY,workflow_id TEXT NOT NULL,campaign_export_id TEXT NOT NULL,video_review_id TEXT NOT NULL,blueprint_id TEXT NOT NULL,workspace_id TEXT NOT NULL,user_id TEXT NOT NULL,version INTEGER NOT NULL,is_latest BOOLEAN NOT NULL DEFAULT TRUE,idempotency_key TEXT NOT NULL,manifest JSONB NOT NULL,sha256 TEXT NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(workflow_id,version),UNIQUE(workspace_id,user_id,idempotency_key));CREATE TABLE IF NOT EXISTS aura_production_delivery_assets(id TEXT PRIMARY KEY,delivery_id TEXT NOT NULL REFERENCES aura_production_deliveries(id) ON DELETE CASCADE,target_id TEXT NOT NULL,kind TEXT NOT NULL,storage_key TEXT NOT NULL,sha256 TEXT NOT NULL,size_bytes INTEGER NOT NULL,width INTEGER NOT NULL,height INTEGER NOT NULL,duration_seconds DOUBLE PRECISION NOT NULL,codec TEXT NOT NULL,container TEXT NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(delivery_id,target_id));CREATE INDEX IF NOT EXISTS idx_aura_delivery_latest ON aura_production_deliveries(workflow_id,is_latest,version DESC);`;
const CONVERSATIONS_SQL=`
CREATE TABLE IF NOT EXISTS aura_agent_conversations(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,workflow_id TEXT REFERENCES aura_agent_workflows(id) ON DELETE SET NULL,title TEXT NOT NULL DEFAULT '',source_mode TEXT,locale TEXT NOT NULL DEFAULT 'ar',status TEXT NOT NULL DEFAULT 'active',message_count INTEGER NOT NULL DEFAULT 0,last_message_at TIMESTAMPTZ,metadata JSONB NOT NULL DEFAULT '{}'::jsonb,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE INDEX IF NOT EXISTS idx_aura_agent_conversation_recent ON aura_agent_conversations(workspace_id,user_id,status,last_message_at DESC NULLS LAST);CREATE INDEX IF NOT EXISTS idx_aura_agent_conversation_workflow ON aura_agent_conversations(workflow_id) WHERE workflow_id IS NOT NULL;
CREATE TABLE IF NOT EXISTS aura_agent_messages(id TEXT PRIMARY KEY,conversation_id TEXT NOT NULL REFERENCES aura_agent_conversations(id) ON DELETE CASCADE,workspace_id TEXT NOT NULL,user_id TEXT NOT NULL,seq INTEGER NOT NULL,role TEXT NOT NULL,content TEXT NOT NULL DEFAULT '',tool_name TEXT,tool_call_id TEXT,tool_calls JSONB,tool_result JSONB,status TEXT NOT NULL DEFAULT 'complete',provider TEXT,model TEXT,prompt_tokens INTEGER,completion_tokens INTEGER,credits_charged INTEGER NOT NULL DEFAULT 0,error_code TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(conversation_id,seq));CREATE INDEX IF NOT EXISTS idx_aura_agent_message_conversation ON aura_agent_messages(conversation_id,seq);CREATE UNIQUE INDEX IF NOT EXISTS idx_aura_agent_message_tool_call ON aura_agent_messages(conversation_id,tool_call_id) WHERE tool_call_id IS NOT NULL;`;
const VIDEO_TEMPLATES_SQL=`
CREATE TABLE IF NOT EXISTS aura_video_templates(id TEXT PRIMARY KEY,workspace_id TEXT REFERENCES workspaces(id) ON DELETE CASCADE,created_by_user_id TEXT REFERENCES users(id) ON DELETE SET NULL,slug TEXT NOT NULL,title TEXT NOT NULL,description TEXT NOT NULL DEFAULT '',category TEXT NOT NULL,subcategory TEXT NOT NULL DEFAULT '',locale TEXT NOT NULL DEFAULT 'ar',aspect_ratio TEXT NOT NULL DEFAULT '9:16',actor_count INTEGER NOT NULL DEFAULT 1,duration_seconds DOUBLE PRECISION NOT NULL DEFAULT 0,scenario_script JSONB NOT NULL DEFAULT '{}'::jsonb,tags JSONB NOT NULL DEFAULT '[]'::jsonb,preview_video_key TEXT,preview_poster_key TEXT,is_system BOOLEAN NOT NULL DEFAULT FALSE,is_promo BOOLEAN NOT NULL DEFAULT FALSE,status TEXT NOT NULL DEFAULT 'active',version INTEGER NOT NULL DEFAULT 1,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE UNIQUE INDEX IF NOT EXISTS idx_aura_video_template_system_slug ON aura_video_templates(slug) WHERE workspace_id IS NULL;CREATE UNIQUE INDEX IF NOT EXISTS idx_aura_video_template_workspace_slug ON aura_video_templates(workspace_id,slug) WHERE workspace_id IS NOT NULL;CREATE INDEX IF NOT EXISTS idx_aura_video_template_browse ON aura_video_templates(category,subcategory,status,updated_at DESC);`;
const MEDIA_CONSENTS_SQL=`
CREATE TABLE IF NOT EXISTS aura_uploaded_media_consents(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,media_key TEXT NOT NULL,media_type TEXT NOT NULL,subject_name TEXT NOT NULL DEFAULT '',consent_type TEXT NOT NULL DEFAULT 'self',consent_status TEXT NOT NULL DEFAULT 'granted',consent_document_key TEXT,consent_locale TEXT NOT NULL DEFAULT 'ar',source TEXT NOT NULL DEFAULT 'upload',ip_address TEXT,user_agent TEXT,metadata JSONB NOT NULL DEFAULT '{}'::jsonb,granted_at TIMESTAMPTZ,revoked_at TIMESTAMPTZ,expires_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE UNIQUE INDEX IF NOT EXISTS idx_aura_media_consent_media ON aura_uploaded_media_consents(workspace_id,media_key);CREATE INDEX IF NOT EXISTS idx_aura_media_consent_lookup ON aura_uploaded_media_consents(workspace_id,user_id,consent_status,updated_at DESC);CREATE INDEX IF NOT EXISTS idx_aura_media_consent_expiry ON aura_uploaded_media_consents(consent_status,expires_at) WHERE expires_at IS NOT NULL;`;
function digest(sql:string){return crypto.createHash('sha256').update(sql).digest('hex')}export const auraMigrations:ReadonlyArray<AuraMigration>=[{id:'012_aura_agent_platform',description:'Centralize Aura V1 workflow and production tables',sql:SQL,checksum:digest(SQL)},{id:'013_aura_agent_conversations',description:'Persistent agent chat conversations and messages',sql:CONVERSATIONS_SQL,checksum:digest(CONVERSATIONS_SQL)},{id:'014_aura_video_templates',description:'Video templates catalog with scenario_script JSONB and browse indexes',sql:VIDEO_TEMPLATES_SQL,checksum:digest(VIDEO_TEMPLATES_SQL)},{id:'015_uploaded_media_consents',description:'Consent records for user-uploaded actor media',sql:MEDIA_CONSENTS_SQL,checksum:digest(MEDIA_CONSENTS_SQL)}];
async function ensureRegistry(client:PoolClient){await client.query(`CREATE TABLE IF NOT EXISTS aura_schema_migrations(id TEXT PRIMARY KEY,description TEXT NOT NULL,checksum TEXT NOT NULL,applied_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`)}export async function runAuraMigrations(pool:Pool):Promise<void>{const client=await pool.connect();try{await client.query('BEGIN');await client.query("SELECT pg_advisory_xact_lock(hashtext('aurapost:aura-schema-migrations'))");await ensureRegistry(client);for(const migration of auraMigrations){const existing=(await client.query<{checksum:string}>('SELECT checksum FROM aura_schema_migrations WHERE id=$1 LIMIT 1',[migration.id])).rows[0];if(existing){if(existing.checksum!==migration.checksum)throw new Error(`Aura migration checksum mismatch: ${migration.id}`);continue;}await client.query(migration.sql);await client.query('INSERT INTO aura_schema_migrations(id,description,checksum) VALUES($1,$2,$3)',[migration.id,migration.description,migration.checksum]);}await client.query('COMMIT');}catch(error){await client.query('ROLLBACK');throw error;}finally{client.release();}}
