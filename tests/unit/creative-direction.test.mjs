import assert from'node:assert/strict';
import test from'node:test';
import{AgentContractError}from'../../server/agent/contracts.ts';
import{assertCreativeDirectionApprovable,normalizeCreativeDirection}from'../../server/agent/creativeDirection.ts';
const evidence=[{id:'product_fact:title',field:'title',type:'product_fact',value:'Aura Lamp',source:'Product'}];
const scene=(durationSeconds)=>({durationSeconds,framing:'Close shot',action:'Show Aura Lamp',cameraMotion:'Slow push',lighting:'Soft side light',overlayText:'Aura Lamp',voiceover:'Meet Aura Lamp',assetPrompt:'Preserve the exact product appearance.',evidenceRefs:['product_fact:title']});
const value={concept:'A calm product-led visual system.',visualIdentity:{moodKeywords:['calm','precise','modern'],palette:[{name:'Ink',hex:'#111827',usage:'Background'},{name:'Mint',hex:'#34d399',usage:'Accent'}],headingStyle:'Bold geometric sans',bodyStyle:'Readable neutral sans',composition:'Product-first negative space',lighting:'Soft directional light',textures:['matte']},productPresentation:{heroShot:'Centered product hero',detailShots:['Detail one','Detail two'],lifestyleScenes:['Scene one','Scene two'],prohibitedAlterations:['Do not change the logo','Do not change product geometry']},platformFrames:[{platform:'TikTok',aspectRatio:'9:16',safeZones:'Keep text away from controls',textDensity:'Low',pacing:'Fast opening'},{platform:'Instagram',aspectRatio:'1:1',safeZones:'Center key content',textDensity:'Low',pacing:'Measured'}],storyboards:[{sourceScriptTitle:'Launch',platform:'TikTok',aspectRatio:'9:16',durationSeconds:15,scenes:[scene(5),scene(5),scene(5)]}],imagePrompts:Array.from({length:3},(_,index)=>({name:`Prompt ${index+1}`,aspectRatio:'1:1',prompt:'Photograph Aura Lamp without changing product details.',negativePrompt:'No altered logo or geometry.',evidenceRefs:['product_fact:title']})),videoDirection:{continuityRules:['Keep product consistent','Keep lighting direction consistent','Keep palette consistent'],transitions:['Cut','Match cut'],audioMood:'Calm modern rhythm',captionStyle:'High contrast captions'},accessibility:{contrastGuidance:'Use readable contrast',captionGuidance:'Caption every spoken line',altTextTemplate:'Aura Lamp in the described scene'},complianceNotes:['Verify product fidelity','Review all overlays before production']};
test('normalizes an approvable direction',()=>{const result=normalizeCreativeDirection(value,evidence);assert.equal(result.storyboards[0].scenes[2].order,3);assert.doesNotThrow(()=>assertCreativeDirectionApprovable(result));});
test('rejects invalid palette colors',()=>{const invalid=structuredClone(value);invalid.visualIdentity.palette[0].hex='navy';assert.throws(()=>normalizeCreativeDirection(invalid,evidence),AgentContractError);});
test('rejects scene timing drift',()=>{const invalid=structuredClone(value);invalid.storyboards[0].scenes[0].durationSeconds=10;assert.throws(()=>normalizeCreativeDirection(invalid,evidence),AgentContractError);});
test('rejects unknown evidence references',()=>{const invalid=structuredClone(value);invalid.imagePrompts[0].evidenceRefs=['missing'];assert.throws(()=>normalizeCreativeDirection(invalid,evidence),AgentContractError);});
