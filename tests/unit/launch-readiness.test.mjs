import test from'node:test';import assert from'node:assert/strict';import{buildLaunchReadiness}from'../../server/agent/launchReadiness.ts';const env=['TEST_MODE','PUBLISHING_ENABLED','SOCIAL_CONNECTIONS_ENABLED','SMART_REPOST_ENABLED','PAID_ADS_ENABLED'];function reset(){for(const key of env)delete process.env[key];process.env.AURAPOST_RELEASE_VERSION='v1';}const complete={checkedAt:'2026-08-15T00:00:00Z',databaseReady:true,workflowExists:true,briefApproved:true,creativeDirectionApproved:true,blueprintApproved:true,mediaCompleted:true,videoCompleted:true,reviewApproved:true,deliveryCreated:true,storageReady:true};test('marks a completed V1 workflow ready',()=>{reset();const value=buildLaunchReadiness(complete);assert.equal(value.status,'ready');assert.deepEqual(value.blockingCodes,[]);});test('fails closed when a V2 capability is enabled',()=>{reset();process.env.PUBLISHING_ENABLED='true';const value=buildLaunchReadiness(complete);assert.equal(value.status,'blocked');assert.ok(value.blockingCodes.includes('PUBLISHING_DISABLED'));});test('reports incomplete production handoffs',()=>{reset();const value=buildLaunchReadiness({...complete,videoCompleted:false,deliveryCreated:false});assert.deepEqual(value.blockingCodes.filter(code=>code.includes('VIDEO')||code.includes('DELIVERY')),['VIDEO_RENDERING_COMPLETED','PRODUCTION_DELIVERY_CREATED']);});
