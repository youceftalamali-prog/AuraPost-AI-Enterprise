import test from'node:test';import assert from'node:assert/strict';import{buildProductionDelivery,renderProductionDeliveryMarkdown}from'../../server/agent/productionDelivery.ts';const asset={id:'asset-1',targetId:'target-1',kind:'revision',sha256:'a'.repeat(64),sizeBytes:1024,width:1080,height:1920,durationSeconds:15,codec:'h264',container:'mp4'};const input={generatedAt:'2026-08-15T00:00:00Z',campaignExportId:'export-1',campaignExportVersion:2,campaignExportSha256:'b'.repeat(64),videoReviewId:'review-1',videoReviewVersion:3,videoReviewSha256:'c'.repeat(64),blueprintSha256:'d'.repeat(64),assets:[asset]};test('builds deterministic production delivery manifests',()=>{const first=buildProductionDelivery(input);const second=buildProductionDelivery(input);assert.deepEqual(first,second);assert.match(first.contentSha256,/^[a-f0-9]{64}$/);assert.equal(first.releasePolicy.publishing,false);});test('requires one approved video per target',()=>{assert.throws(()=>buildProductionDelivery({...input,assets:[asset,{...asset,id:'asset-2'}]}),/one validated video per target/);});test('renders integrity metadata without publishing claims',()=>{const markdown=renderProductionDeliveryMarkdown(buildProductionDelivery(input));assert.match(markdown,/Approved Videos/);assert.match(markdown,/No content was published/);});
