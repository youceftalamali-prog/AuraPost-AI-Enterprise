import test from'node:test';import assert from'node:assert/strict';import{approveExecutedVideoReview,resolveVideoReviewAnnotation,selectCurrentVideoVersions}from'../../server/agent/videoReviewApproval.ts';
const base={status:'changes_requested',annotations:[{id:'a1',resolved:false}],revisionRequests:[{id:'r1'}],updatedAt:'2026-08-14T20:00:00Z',contentSha256:'a'};test('resolves annotations immutably',()=>{const next=resolveVideoReviewAnnotation(base,{annotationId:'a1',resolvedAt:'2026-08-14T20:01:00Z'});assert.equal(next.annotations[0].resolved,true);assert.equal(base.annotations[0].resolved,false);});test('requires successful revisions before approval',()=>{const resolved={...base,annotations:[{id:'a1',resolved:true}]};assert.throws(()=>approveExecutedVideoReview(resolved,{completedRevisionIds:[],approvedAt:'2026-08-14T20:02:00Z'}),/must succeed/);assert.equal(approveExecutedVideoReview(resolved,{completedRevisionIds:['r1'],approvedAt:'2026-08-14T20:02:00Z'}).status,'approved');});test('marks latest target version current without deleting originals',()=>{const versions=selectCurrentVideoVersions([{id:'o',targetId:'t',kind:'original',sourceId:'o',createdAt:'2026-08-14T20:00:00Z',sha256:null,sizeBytes:null,videoUrl:null,current:false},{id:'r',targetId:'t',kind:'revision',sourceId:'o',createdAt:'2026-08-14T20:01:00Z',sha256:null,sizeBytes:null,videoUrl:null,current:false}]);assert.equal(versions.find(item=>item.id==='o').current,false);assert.equal(versions.find(item=>item.id==='r').current,true);});
