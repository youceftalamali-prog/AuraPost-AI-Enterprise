import test from'node:test';import assert from'node:assert/strict';import{buildVideoSceneRevisionPlan,VIDEO_SCENE_REVISION_SCHEMA}from'../../server/agent/videoSceneRevision.ts';
const target={id:'target-1',width:1080,height:1920,durationFrames:450,scenes:[{id:'scene-1'}]};const renderPlan={targetId:'target-1',width:1080,height:1920,durationFrames:450,estimatedCredits:10};const base={id:'revision-1',sourceRenderJobId:'job-1',targetId:'target-1',sceneId:'scene-1',reason:'Improve scene',preserveDuration:true,preserveProduct:true,status:'planned',contentSha256:'a'.repeat(64)};test('prices caption-only rerender without image credits',()=>{const plan=buildVideoSceneRevisionPlan({...base,replacementOverlayText:'New caption'},target,renderPlan);assert.equal(plan.schemaVersion,VIDEO_SCENE_REVISION_SCHEMA);assert.equal(plan.aiCredits,0);assert.equal(plan.videoCredits,10);assert.equal(plan.totalCredits,10);});test('prices visual regeneration and rerender separately',()=>{const plan=buildVideoSceneRevisionPlan({...base,replacementPrompt:'New lighting'},target,renderPlan);assert.equal(plan.aiCredits,20);assert.equal(plan.videoCredits,10);assert.equal(plan.totalCredits,30);assert.equal(plan.preserveProduct,true);});test('rejects revisions outside the approved target',()=>{assert.throws(()=>buildVideoSceneRevisionPlan({...base,sceneId:'missing',replacementPrompt:'New lighting'},target,renderPlan),/approved target, scene/);});
